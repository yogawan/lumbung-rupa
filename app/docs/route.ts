export async function GET() {
  // Minimal placeholder for documentation route to satisfy type checks.
  // Real documentation files will be placed under `/docs` later.
  return new Response('Documentation placeholder', { headers: { 'Content-Type': 'text/plain' } })
}
